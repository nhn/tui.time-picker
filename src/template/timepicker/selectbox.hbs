<select class="tui-timepicker-select" aria-label="Time">
    {{#each items as |item|}}
        {{#if ([../helpers/equals] ../initialValue item)}}
            <option value="{{item}}" selected {{lookup ../disabledItems @index}}>{{item}}</option>
        {{else}}
            <option value="{{item}}" {{lookup ../disabledItems @index}}>{{item}}</option>
        {{/if}}
    {{/each}}
</select>
